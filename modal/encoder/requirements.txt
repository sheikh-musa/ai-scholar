fastapi==0.115.0
uvicorn[standard]==0.32.0
sentence-transformers==3.2.1
# torch CPU build — Modal will swap to GPU image if/when ENCODER-EVAL winner needs it
torch==2.5.1 --index-url https://download.pytorch.org/whl/cpu
# BGE-M3 + jina-v3 both ship via sentence-transformers + huggingface; no model-specific deps needed
huggingface-hub==0.27.1
numpy==2.1.3
